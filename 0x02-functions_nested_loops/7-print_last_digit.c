#include"main.h"
/**
 * print_last_digit - it print last digit of a
 *				number
 * @n: Check the input
 *
 * Return: the last digit
 */
int print_last_digit(int n)
{
	int ld = n % 10;

	return (ld);
}
